{
  "scripts": {
    "deploy": "dotenv -- ./node_modules/.bin/serverless deploy",
    "package": "dotenv -- ./node_modules/.bin/serverless package",
    "teardown": "dotenv -- ./node_modules/.bin/serverless remove",
    "print:aws-policy": "dotenv -- node ./misc/aws-policy.template.js"
  },
  "devDependencies": {
    "dotenv-cli": "^2.0.0",
    "serverless": "^1.45.1"
  }
}
